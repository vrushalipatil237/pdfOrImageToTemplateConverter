# pdfOrImageToTemplateConverter
Share PDF or Image to convert it to template format of HTML and CSS

# PDF / Image to HTML (Java PDF Compatible)

This Streamlit app converts PDF or Image files into HTML that is fully
compatible with Java HTML → PDF converters such as:

- OpenHTMLtoPDF
- Flying Saucer
- iText (html2pdf)

## Run
pip install -r requirements.txt
streamlit run app.py

## Notes
- Uses absolute positioning
- Uses px units only
- No JavaScript
- No flex/grid
- Safe for Java PDF engines
